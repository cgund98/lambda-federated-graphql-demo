{
  "scripts": {
    "build": "tsc -noEmit && node build.js"
  },
  "name": "pulumi-serverless-aws-typescript",
  "devDependencies": {
    "@types/node": "^18",
    "esbuild": "0.20.2"
  },
  "dependencies": {
    "@apollo/server": "^4.10.2",
    "@as-integrations/aws-lambda": "^3.1.0",
    "graphql": "^16.8.1",
    "typescript": "^4.6.2"
  }
}
